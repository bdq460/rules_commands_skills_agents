# Generate Git New Release Note

根据 Git 提交历史自动生成版本发布说明文档，用于 GitHub Release。

> **重要说明**：本文档定义了生成版本发布说明的标准流程和规范。
> **只有在用户明确调用 `/generate_git_new_release_note` 命令时，AI 才能执行生成和保存版本发布文档的操作**。
> 在未收到该命令的情况下，AI **不允许执行任何实际的文档生成和保存操作**。

## 功能说明

提供基于 Git 历史的版本发布说明生成功能：

### 核心规范

- **遵循 GitHub Release Notes 格式**：使用主流的发布说明格式
- **中英双语版本**：必须生成中文和英文两套完整文档
- **全面展示变更**：确保涵盖所有 feature 变更和核心修改
- **Markdown 规范**：符合 markdownlint 规范，代码块指定语言，无重复标题

### 自动化要求

- **自动分析 Git 历史**：获取从上一个版本 tag 到当前 HEAD 的所有提交
- **按 Conventional Commits 分类**：将提交按类型分类
- **统计变更文件**：列出所有变更的文件及其变更类型
- **自查和优化**：生成后检查内容完整性、格式正确性，进行两轮优化改进

### 交互流程

> **执行条件**：以下交互流程仅在用户明确调用 `/generate_git_new_release_note` 命令时执行。
> **只有在收到该命令的情况下，AI 才能执行生成和保存版本发布文档的操作**。

- **展示预览**：生成后展示中文和英文版本的预览
- **支持修改**：允许用户在确认前修改或重新生成内容
- **保存确认**：确认后才保存文件到 `doc/versions/<version>/` 目录

## 使用方法

### 命令格式

```text
/generate_git_new_release_note [start_ref] [end_ref]
```

### 参数类型

参数使用前缀来明确区分类型：

| 前缀 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `tag:` | Git tag | 明确的 tag 名称 | `tag:V1.0.2`, `tag:v1.0.1` |
| `commit:` | Commit hash | 明确的 commit hash | `commit:abc1234`, `commit:def5678` |
| `version:` | 版本号 | 仅用于单参数，指定输出版本号 | `version:1.0.3` |
| 无前缀 | 关键字 | 特殊引用 | `last-tag`, `current`, `head` |

#### 关键字列表

| 关键字 | 说明 | 适用参数 |
|--------|------|----------|
| `last-tag` / `latest-tag` | 最新的 Git tag | 参数 1 |
| `current` / `head` / `now` | 当前工作目录（HEAD） | 参数 2 |
| `last-commit` / `latest` | 最新的 commit（HEAD） | 参数 2 |

#### 参数 1: `start_ref`（起始参考点）

- **默认值**: `last-tag`
- **格式**: `[tag:|commit:]<value>` 或 `<keyword>`

#### 参数 2: `end_ref`（结束参考点）

- **默认值**: `current`
- **格式**: `[tag:|commit:]<value>` 或 `<keyword>`

### 使用示例

```text
# 默认：最后一个 tag vs 当前代码
/generate_git_new_release_note

# 指定输出版本号（从最后一个 tag 到当前代码）
/generate_git_new_release_note version:1.0.3

# 指定起始和结束 tag
/generate_git_new_release_note tag:V1.0.2 tag:V1.0.3

# 从指定 commit 到当前代码
/generate_git_new_release_note commit:abc1234 current

# 从最后一个 tag 到指定 commit
/generate_git_new_release_note last-tag commit:def5678

# 两个 commit 之间
/generate_git_new_release_note commit:abc1234 commit:def5678

# 混合使用
/generate_git_new_release_note tag:V1.0.2 commit:abc1234
/generate_git_new_release_note commit:abc1234 tag:V1.0.3

# 使用关键字
/generate_git_new_release_note last-tag current
/generate_git_new_release_note tag:V1.0.2 head
```

### 参数解析规则

1. **无参数情况**：
   - `start_ref` = `last-tag`（最新的 Git tag）
   - `end_ref` = `current`（HEAD）
   - 版本号从 `end_ref` 推导并递增

2. **单参数情况**：
   - 如果参数以 `version:` 开头（如 `version:1.0.3`），则作为输出版本号，比较范围为 `last-tag..current`
   - 如果参数是 `tag:<name>`，则作为 `start_ref`，`end_ref` 默认为 `current`
   - 如果参数是 `commit:<hash>`，则作为 `start_ref`，`end_ref` 默认为 `current`
   - 如果参数是关键字（如 `last-tag`），则作为 `start_ref`，`end_ref` 默认为 `current`

3. **双参数情况**：
   - 第一个参数解析为 `start_ref`（支持 `tag:`, `commit:`, 关键字）
   - 第二个参数解析为 `end_ref`（支持 `tag:`, `commit:`, 关键字）
   - 生成版本号根据 `end_ref` 自动推导（如果是 tag）或需要用户指定

### 类型前缀说明

- **`tag:`** - 明确指定为 Git tag，例如：`tag:V1.0.2`
- **`commit:`** - 明确指定为 commit hash，例如：`commit:abc1234`
- **`version:`** - 仅用于单参数，指定生成的版本号，例如：`version:1.0.3`
- **无前缀** - 识别为关键字，例如：`last-tag`, `current`

## 执行流程

### 标准流程

```text
1. 获取 Git 历史
   git log <start_ref>..<end_ref>
   git diff --name-only <start_ref>..<end_ref>

2. 分析变更内容
   按 Conventional Commits 规范分类提交
   统计文件变更数量
   识别核心功能变更

3. 生成文档内容
   生成中文版本
   生成英文版本

4. 格式检查和优化
   检查 Markdown 格式
   运行 markdownlint
   优化内容和表达

5. 展示预览
   展示中文版本
   展示英文版本

6. 交互式确认
   用户确认或修改

7. 保存文件
   创建版本目录
   保存中英文版本
```

### 步骤 1: 获取 Git 历史

根据参数类型获取 Git 历史：

```bash
# 获取最新的 tag
git describe --tags --abbrev=0

# 获取提交消息（从 start_ref 到 end_ref）
git log <start_ref>..<end_ref> --oneline

# 获取变更文件列表
git diff --name-only <start_ref>..<end_ref>

# 获取变更统计
git diff --stat <start_ref>..<end_ref>
```

参数解析示例：

```bash
# 示例 1: /generate_git_new_release_note
# start_ref = $(git describe --tags --abbrev=0), end_ref = HEAD
git log $(git describe --tags --abbrev=0)..HEAD --oneline

# 示例 2: /generate_git_new_release_note 1.0.3
# 版本号 = 1.0.3, start_ref = $(git describe --tags --abbrev=0), end_ref = HEAD
git log V1.0.2..HEAD --oneline

# 示例 3: /generate_git_new_release_note V1.0.2 V1.0.3
# start_ref = V1.0.2, end_ref = V1.0.3
git log V1.0.2..V1.0.3 --oneline

# 示例 4: /generate_git_new_release_note abc1234 current
# start_ref = abc1234, end_ref = HEAD
git log abc1234..HEAD --oneline
```

### 步骤 2: 分析变更内容（意图识别与聚合）

**重要：不是简单罗列提交记录，而是进行内容整理、意图识别和意图聚合**

#### 2.1 按照 Conventional Commits 规范分类提交

- `feat` - 新功能
- `fix` - 修复 bug
- `docs` - 文档更新
- `refactor` - 重构
- `chore` - 构建/工具链更新
- `test` - 测试相关

#### 2.2 意图识别（Intent Recognition）

分析每个提交背后的真实意图：

| 提交消息 | 表面描述 | 真实意图 |
|---------|---------|---------|
| `feat: add code action provider` | 添加代码动作提供器 | 提供快捷的格式化操作入口 |
| `fix: fix virtual document diagnostic residue` | 修复虚拟文档诊断残留 | 解决格式化后诊断信息不更新的问题 |
| `refactor: optimize formatter` | 优化格式化器 | 提升大文件格式化性能 |

#### 2.3 意图聚合（Intent Aggregation）

将相关提交聚合为功能特性：

**示例 1：代码动作提供器功能**

```text
相关提交:
- feat: add code action provider
- feat: register code action in extension
- docs: add code action usage guide

聚合结果:
- 添加代码动作提供器: 提供快捷的格式化操作，用户可以通过右键菜单或快捷键触发格式化
```

**示例 2：性能优化**

```text
相关提交:
- refactor: optimize formatter algorithm
- fix: fix large file timeout issue
- test: add performance tests

聚合结果:
- 优化格式化性能: 重构格式化算法，解决大文件格式化超时问题，提升处理速度
```

#### 2.4 新特性判定标准

**新特性（New Features）判定原则**：

以下类型的变更应归类为**新特性**：

| 变更类型 | 判定标准 | 示例 |
|----------|----------|------|
| **新增功能模块** | 添加全新的功能模块或组件 | 添加代码动作提供器、添加性能监控面板 |
| **国际化支持** | 新增多语言/国际化功能 | 添加 i18n 支持、新增 17 种语言界面 |
| **新配置选项** | 新增用户可配置的功能选项 | 新增格式化配置项、新增主题设置 |
| **新命令/操作** | 新增用户可执行的命令或操作 | 新增右键菜单命令、新增快捷键 |
| **新集成** | 与外部系统或服务的新集成 | 新增 GitHub 集成、新增云同步 |
| **可见功能改进** | 对现有功能的显著增强（用户可见） | 新增文件类型支持、新增格式化规则 |

**不应归类为新特性的变更**：

- 架构重构（无用户可见功能变化）
- 性能优化（属于质量改进）
- Bug 修复（属于问题修复）
- 文档更新
- 代码清理、测试添加

**判定口诀**：
> **用户能感知、能使用、能配置的新能力 = 新特性**

#### 2.5 变更分析维度

1. **功能维度**：新增功能、功能改进、功能移除
2. **架构维度**：架构调整、模块重构、依赖变更
3. **质量维度**：性能优化、稳定性提升、安全性增强
4. **体验维度**：用户体验、开发者体验、交互优化
5. **文档维度**：文档完善、示例更新、API 文档

### 步骤 3: 生成文档内容（GitHub Release 正式格式）

生成符合 GitHub Release 主流格式的文档，包含以下章节：

#### 3.1 更新摘要（What's Changed）

用 2-3 句话概括本版本的核心价值：

```text
本版本主要添加了代码动作提供器功能，优化了格式化性能，并修复了多个已知问题。
重点提升了用户体验和系统稳定性。
```

#### 3.2 变更情况（Change Overview）

**变更级别**（根据变更程度自动评估）：

| 级别 | 说明 | 评估标准 |
|------|------|----------|
| 🔴 **重大变更** | 架构重构、破坏性变更、核心功能重写 | 涉及架构调整、API 变更、依赖升级 |
| 🟡 **中等变更** | 新功能添加、性能优化、重要修复 | 新增功能模块、显著性能提升、关键 bug 修复 |
| 🟢 **轻微变更** | 文档更新、配置调整、小修复 | 文档完善、配置优化、小问题修复 |

**变更类型列表**：

- 🆕 **新功能**: X 个
- 🐛 **问题修复**: X 个
- ⚡ **性能优化**: X 个
- 📚 **文档更新**: X 个
- 🏗️ **架构重构**: X 个
- 🔧 **其他**: X 个

**代码统计**：

- 变更文件: X 个
- 代码变更: +X, -X

#### 3.3 变更列表（Change List）

变更列表用于简单列举所有变更内容，按类型分组展示。

**格式要求**：

- 使用简洁的描述，一句话概括变更内容
- 包含相关提交 hash（可选）
- 按类型分组：新特性、问题修复、架构调整、文档更新等

**示例**：

```text
## 变更列表

### 新特性

- 添加代码动作提供器 (abc1234): 支持右键菜单和快捷键触发格式化
- 支持自定义格式化选项 (def5678): 可配置缩进大小、换行风格等
- 新增多语言支持 (ghi9012): 支持 17 种语言界面

### 问题修复

- 修复虚拟文档诊断残留 (jkl3456): 解决 AI 助手导致的诊断问题
- 修复大文件格式化超时 (mno7890): 优化大文件处理性能

### 架构调整

- 迁移到 ES Modules (pqr1122): 全面升级模块系统
- 重构配置系统 (stu3344): 统一配置管理接口

### 文档更新

- 新增开发者文档体系 (vwx5566): 添加 10 篇专业文档
- 更新 README 使用说明 (yz7788): 完善快速开始指南
```

#### 3.4 变更详情（Change Details）

变更详情用于对有必要展开的变更进行详细描述。以下情况需要展开：

- **配置相关变更**: 需要列出具体配置项、可选值、默认值
- **多语言支持**: 需要列出支持的语言列表
- **API 变更**: 需要说明接口变化、参数说明
- **依赖升级**: 需要说明版本变化、兼容性影响
- **性能优化**: 需要提供量化数据对比

**不需要展开的变更**:

- 简单的 bug 修复（已在变更列表中说明）
- 文档格式调整
- 代码重构（无用户可见影响）

**示例 1：多语言支持详情**

```text
### 多语言支持详情

**变更** (ghi9012): 新增国际化支持，界面支持 17 种语言

**支持语言列表**:
| 语言代码 | 语言名称 | 备注 |
|----------|----------|------|
| en | English | 默认语言 |
| zh | 简体中文 | 完全支持 |
| zh-tw | 繁體中文 | 完全支持 |
| ja | 日本語 | 完全支持 |
| ko | 한국어 | 完全支持 |
| de | Deutsch | 完全支持 |
| fr | Français | 完全支持 |
| es | Español | 完全支持 |
| it | Italiano | 完全支持 |
| pt | Português | 完全支持 |
| ru | Русский | 完全支持 |
| ar | العربية | RTL 支持 |
| hi | हिन्दी | 完全支持 |
| pl | Polski | 完全支持 |
| tr | Türkçe | 完全支持 |
| vi | Tiếng Việt | 完全支持 |
| th | ไทย | 完全支持 |

**配置方式**:
```json
{
  "shellFormatter.language": "zh"
}
```

**相关提交**: ghi9012

**示例 2：配置项变更详情**

```text
### 格式化配置选项详情

**变更** (def5678): 扩展格式化配置，支持更多自定义选项

**新增配置项**:

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `shellFormatter.indentSize` | number | 4 | 缩进空格数 |
| `shellFormatter.useTabs` | boolean | false | 是否使用 Tab 缩进 |
| `shellFormatter.lineEnding` | string | "lf" | 换行符风格 (lf/crlf/auto) |
| `shellFormatter.maxLineLength` | number | 120 | 最大行长度限制 |

**配置示例**:
```json
{
  "shellFormatter.indentSize": 2,
  "shellFormatter.useTabs": false,
  "shellFormatter.lineEnding": "auto"
}
```

**示例 3：性能优化详情**

```text
### 大文件格式化性能优化详情

**变更** (mno7890): 优化大文件格式化算法，提升处理速度

**性能对比**:

| 文件大小 | 优化前 | 优化后 | 提升幅度 |
|----------|--------|--------|----------|
| 1000 行 | 5.2s | 1.2s | 77% |
| 5000 行 | 28.5s | 6.8s | 76% |
| 10000 行 | 62.3s | 14.5s | 77% |

**优化策略**:
1. 优化正则匹配算法，减少回溯
2. 使用流式处理替代全量加载
3. 缓存频繁使用的正则表达式
```

#### 3.5 文档更新（Documentation）

- 新增文档章节
- 更新的使用示例
- API 文档更新

### 步骤 4: 格式检查和优化

```bash
# 运行 markdownlint 检查
npx markdownlint doc/versions/1.0.3/v1.0.3_CN.md
npx markdownlint doc/versions/1.0.3/v1.0.3_EN.md

# 自动修复可修复的错误
npx markdownlint --fix doc/versions/1.0.3/*.md
```

### 步骤 5: 自查和优化（两轮深度检查）

#### 第一轮检查：内容完整性检查

**Feature 覆盖检查**：

- [ ] 是否涵盖了所有用户可见的功能变更？
- [ ] 是否遗漏了重要的架构调整？
- [ ] 性能优化是否被充分记录？
- [ ] 破坏性变更是否被明确标注？

**意图聚合检查**：

- [ ] 是否对提交进行了意图识别？
- [ ] 相关提交是否被正确聚合？
- [ ] 功能描述是否准确反映真实意图？
- [ ] 是否避免了简单罗列提交消息？

**全面性检查**：

- [ ] 新功能是否都有详细说明？
- [ ] 问题修复是否说明了影响和解决方案？
- [ ] 架构变更是否解释了动机和收益？
- [ ] 变更文件列表是否完整？

#### 第二轮优化：表达和格式优化

**内容优化**：

- [ ] 标题是否清晰准确？
- [ ] 描述是否简洁明了？
- [ ] 技术细节是否适度（不过多也不过少）？
- [ ] 中英文表达是否专业自然？

**格式优化**：

- [ ] 是否符合 markdownlint 规范？
- [ ] 代码块是否都指定了语言？
- [ ] 列表格式是否正确？
- [ ] 标题层级是否合理？

**GitHub Release 格式检查**：

- [ ] 是否使用了表情符号标记？
- [ ] 章节组织是否符合主流格式？
- [ ] 是否包含必要的元信息（版本、日期等）？
- [ ] 是否适合直接发布到 GitHub Release？

### 步骤 6: 展示预览

```text
版本发布说明预览 (v1.0.3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━

中文版本: doc/versions/1.0.3/v1.0.3_CN.md
英文版本: doc/versions/1.0.3/v1.0.3_EN.md

变更情况:
  变更级别: 🔴 重大变更

  变更类型:
    🆕 新功能: 2 个
    🐛 问题修复: 1 个
    🏗️ 架构重构: 6 个
    📚 文档更新: 14 个
    🔧 其他: 3 个

  代码统计:
    变更文件: 186 个
    代码变更: +21340, -4978

  重要变更:
    1. 迁移到 ES Modules - 模块系统全面升级
    2. 诊断残留问题修复 - 解决虚拟文档诊断问题
    3. 重构项目文档体系 - 新增 10 篇开发者文档

请选择操作:
  [1] 保存文档
  [2] 查看详细内容
  [3] 重新生成
  [4] 取消操作
```

### 步骤 7: 保存文件

```bash
# 创建版本目录
mkdir -p doc/versions/1.0.3

# 保存中文版本
# doc/versions/1.0.3/v1.0.3_CN.md

# 保存英文版本
# doc/versions/1.0.3/v1.0.3_EN.md
```

## 版本文档结构

### 中文版本模板

```text
# Shell Formatter v1.0.3

## 更新摘要

简要说明本版本的主要更新内容。

## 变更情况

**变更级别**: 🟡 中等变更

**变更类型**:
- 🆕 新功能: 2 个
- 🐛 问题修复: 3 个
- 📚 文档更新: 1 个

**代码统计**:
- 变更文件: 12 个
- 代码变更: +XXX, -XXX

## 变更列表

### 新特性

- 新特性1 (abc1234): 简要描述
- 新特性2 (def5678): 简要描述

### 问题修复

- 修复问题1 (ghi9012): 简要描述
- 修复问题2 (jkl3456): 简要描述

### 架构调整

- 架构调整1 (mno7890): 简要描述

### 文档更新

- 文档更新1 (pqr1122): 简要描述

## 变更详情

### 新特性2详情

**变更** (def5678): 新增多语言支持

**支持语言列表**:
| 语言代码 | 语言名称 |
|----------|----------|
| en | English |
| zh | 简体中文 |
| ... | ... |

**配置方式**:
```json
{
  "shellFormatter.language": "zh"
}
```

### 英文版本模板

```text
# Shell Formatter v1.0.3

## Summary

Brief description of major updates in this version.

## Change Overview

**Change Level**: 🟡 Medium Change

**Change Types**:
- 🆕 New Features: 2
- 🐛 Bug Fixes: 3
- 📚 Documentation Updates: 1

**Code Statistics**:
- Changed Files: 12
- Code Changes: +XXX, -XXX

## Change List

### New Features

- New feature 1 (abc1234): Brief description
- New feature 2 (def5678): Brief description

### Bug Fixes

- Fix issue 1 (ghi9012): Brief description
- Fix issue 2 (jkl3456): Brief description

### Architecture Changes

- Architecture change 1 (mno7890): Brief description

### Documentation

- Documentation update 1 (pqr1122): Brief description

## Change Details

### New Feature 2 Details

**Change**: Add multi-language support

**Supported Languages**:
| Language Code | Language Name |
|---------------|---------------|
| en | English |
| zh | 简体中文 |
| ... | ... |

**Configuration**:
```json
{
  "shellFormatter.language": "en"
}
```

## Markdown 规范

### 必须遵守的规则

1. **MD024 - 无重复标题**

   每个标题内容必须唯一，使用限定词区分相同含义的标题。

2. **MD040 - 代码块指定语言**

   所有代码块必须指定语言类型，未知语言使用 `text`。

3. **MD032 - 列表空行规范**

   列表前后必须有空行，除非列表紧跟在标题后。

4. **MD022 - 标题后空行**

   标题后必须有空行。

5. **MD041 - 首行标题**

   文档第一行必须是标题。

### 代码块语言速查表

- TypeScript 代码：`typescript` / `ts`
- Shell 脚本：`bash` / `sh`
- JSON 数据：`json`
- 普通文本：`text`

### 格式示例

```text
# 标题

## 更新摘要

简要说明本版本的主要更新内容。

## 变更统计

- 新功能: 3 个
- 问题修复: 5 个

## 新特性

- 添加代码动作提供器 (abc1234): 提供快捷的格式化操作

```bash
npm install
```

## 问题修复

- 修复虚拟文档诊断残留 (def5678): 解决诊断信息不更新的问题

## 变更文件列表

- src/formatter.ts - 重构格式化逻辑
- src/config.ts - 添加新配置项

## 注意事项

### 执行前检查

- 确保工作区是干净的（无未提交变更）
- 确保上一个版本的 tag 存在
- 确保有足够的 Git 历史

### 内容要求

- **不是照抄提交消息**：必须进行意图识别和聚合，总结提炼核心价值
- **全面展示 feature**：涵盖所有用户可见的功能变更和核心修改
- **正式专业**：使用 GitHub Release 主流格式，适合直接发布
- **中英双语**：生成完整的中英文版本，语言自然专业
- **量化描述**：性能优化等提供具体数据支撑
- **用户视角**：从用户角度描述变更的价值和影响

### 质量保证

- 生成的文档需要人工审核后使用
- 运行 markdownlint 检查并修复错误
- 进行两轮自查和优化

### 文件保存

- 保存到 `doc/versions/<version>/` 目录
- 文件名格式：`v<version>_CN.md` 和 `v<version>_EN.md`
- 确保目录存在

## 示例输出

### 中文版本示例

```text
# Shell Formatter v1.0.3

## 更新摘要

本版本添加了代码动作提供器、优化了格式化性能，并修复了多个已知问题。

## 变更情况

**变更级别**: 🟡 中等变更

**变更类型**:
- 🆕 新功能: 2 个
- 🐛 问题修复: 3 个
- 📚 文档更新: 1 个

**代码统计**:
- 变更文件: 12 个

## 变更列表

### 新特性

- 添加代码动作提供器 (abc1234): 支持右键菜单和快捷键触发格式化
- 支持自定义格式化选项 (def5678): 可配置缩进大小、换行风格等
- 新增多语言支持 (ghi9012): 支持 17 种语言界面

### 问题修复

- 修复虚拟文档诊断残留 (jkl3456): 解决 AI 助手导致的诊断问题
- 修复大文件格式化超时 (mno7890): 优化大文件处理性能

### 架构调整

- 迁移到 ES Modules (pqr1122): 全面升级模块系统

### 文档更新

- 新增开发者文档体系 (stu3344): 添加 10 篇专业文档

## 变更详情

### 多语言支持详情

**变更** (ghi9012): 新增国际化支持，界面支持 17 种语言

**支持语言列表**:
| 语言代码 | 语言名称 | 备注 |
|----------|----------|------|
| en | English | 默认语言 |
| zh | 简体中文 | 完全支持 |
| ja | 日本語 | 完全支持 |
| ko | 한국어 | 完全支持 |
| de | Deutsch | 完全支持 |
| fr | Français | 完全支持 |

**配置方式**:
```json
{
  "shellFormatter.language": "zh"
}
```

### 自定义格式化选项详情

**变更** (def5678): 扩展格式化配置，支持更多自定义选项

**新增配置项**:

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| shellFormatter.indentSize | number | 4 | 缩进空格数 |
| shellFormatter.useTabs | boolean | false | 是否使用 Tab 缩进 |
| shellFormatter.lineEnding | string | "lf" | 换行符风格 |

### 英文版本示例

```text
# Shell Formatter v1.0.3

## Summary

This version adds code action provider, optimizes formatting performance,
and fixes several known issues.

## Change Overview

**Change Level**: 🟡 Medium Change

**Change Types**:
- 🆕 New Features: 2
- 🐛 Bug Fixes: 3
- 📚 Documentation Updates: 1

**Code Statistics**:
- Changed Files: 12

## Change List

### New Features

- Add code action provider (abc1234): Support right-click menu and keyboard shortcuts
- Support custom formatting options (def5678): Configure indent size, line ending, etc.
- Add multi-language support (ghi9012): Support 17 languages

### Bug Fixes

- Fix virtual document diagnostic residue (jkl3456): Resolve AI assistant diagnostic issues
- Fix large file formatting timeout (mno7890): Optimize large file processing

### Architecture Changes

- Migrate to ES Modules (pqr1122): Upgrade module system

### Documentation

- Add developer documentation system (stu3344): Add 10 professional documents

## Change Details

### Multi-language Support Details

**Change** (ghi9012): Add internationalization support with 17 languages

**Supported Languages**:
| Language Code | Language Name | Note |
|---------------|---------------|------|
| en | English | Default |
| zh | 简体中文 | Full support |
| ja | 日本語 | Full support |
| ko | 한국어 | Full support |
| de | Deutsch | Full support |
| fr | Français | Full support |

**Configuration**:
```json
{
  "shellFormatter.language": "en"
}
```

### Custom Formatting Options Details

**Change** (def5678): Extend formatting configuration with more options

**New Configuration Items**:

| Configuration | Type | Default | Description |
|---------------|------|---------|-------------|
| shellFormatter.indentSize | number | 4 | Indent spaces |
| shellFormatter.useTabs | boolean | false | Use tabs for indent |
| shellFormatter.lineEnding | string | "lf" | Line ending style |

## 常见问题

### 问题 1: 如何获取最新的 tag？

```bash
# 获取最新的 tag
git describe --tags --abbrev=0

# 或
git tag -l "V*" | sort -V | tail -1
```

### 问题 2: 如何查看两个版本之间的所有提交？

```bash
# 查看简要提交信息
git log V1.0.2..HEAD --oneline

# 查看详细提交信息
git log V1.0.2..HEAD --format="%H %s%n%b"

# 按类型分类查看
git log V1.0.2..HEAD --grep="^feat" --oneline
git log V1.0.2..HEAD --grep="^fix" --oneline
```

### 问题 3: 如何统计变更文件？

```bash
# 查看变更文件列表
git diff --name-only V1.0.2..HEAD

# 查看变更统计
git diff --stat V1.0.2..HEAD

# 按文件类型统计
git diff --name-only V1.0.2..HEAD | cut -d. -f2 | sort | uniq -c
```

### 问题 4: 如何修复 markdownlint 错误？

```bash
# 检查错误
npx markdownlint doc/versions/1.0.3/*.md

# 自动修复
npx markdownlint --fix doc/versions/1.0.3/*.md

# 手动修复常见错误
# MD024: 重命名重复的标题
# MD040: 为代码块添加语言标记
# MD032: 在列表前后添加空行
```

## 参考资源

- [Conventional Commits](https://www.conventionalcommits.org/)
- [GitHub Release Notes Best Practices](https://docs.github.com/en/repositories/releasing-projects-on-github/automatically-generated-release-notes)
- [markdownlint 规则文档](https://github.com/DavidAnson/markdownlint/blob/v0.40.0/doc/Rules.md)
- [Markdown 指南](https://www.markdownguide.org/)
